# plainnote.io

The website for [Plainnote](https://github.com/plainnoteio/app), a minimal markdown notes app for Mac.

Plain HTML and CSS. No build step, no dependencies.

## Preview locally

```bash
python3 -m http.server 4173
```

Then open http://localhost:4173.